package com.example.inventoryallocation.repository;

import com.example.inventoryallocation.entity.GroupItemSales;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GroupItemSalesRepository extends JpaRepository<GroupItemSales, Long> {
}